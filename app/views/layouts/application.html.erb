<!DOCTYPE html>
<html class="h-full bg-gradient-to-r from-rose-400 via-fuchsia-500 to-indigo-500">
  <head>
    <title>BookTinder</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head> 
  <!-- This example requires Tailwind CSS v2.0+ -->
  <body class="h-full">
  <div>
    <div>
      <img class="h-32 w-full object-cover lg:h-52" src="https://cdn.pixabay.com/photo/2017/09/11/15/34/animals-2739386_960_720.jpg" alt="">
    </div>
    <div class="max-w-8xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="-mt-12 lg:-mt-16 sm:flex lg:items-end sm:space-x-5">
        <div class="mt-6 sm:flex-1 sm:min-w-0 sm:flex sm:items-center sm:justify-end sm:space-x-6 sm:pb-1">
          <div class="lg:hidden md:block mt-6 min-w-0 flex-1">
            <h1 class="text-2xl font-bold text-gray-900 truncate">Book Tinder</h1>
          </div>
          <div class="mt-6 flex flex-col justify-stretch space-y-3 sm:flex-row sm:space-y-0 sm:space-x-4">
            <%= link_to "Profile", "/profile", class: "inline-flex justify-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-pink-500" %>
            <%= button_to "Sign out", destroy_user_session_path, class: "inline-flex justify-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-pink-500", method: :delete %>
          </div>
        </div>
      </div>
    </div>
    <!-- Static sidebar for desktop -->
    <div class="hidden md:flex md:w-64 md:flex-col md:fixed md:inset-y-0 ">
      <!-- Sidebar component, swap this element with another sidebar if you like -->
      <!-- This example requires Tailwind CSS v2.0+ -->
      <div class="flex flex-col flex-grow border-r border-gray-200 pt-5 pb-4 bg-white overflow-y-auto bg-gradient-to-r from-rose-100 to-teal-100">
        <div class="flex items-center flex-shrink-0 px-4 space-y-5">
          <img class="h-8 w-auto" src="https://freesvg.org/img/blackcat2.png" alt="BookTinder">
        </div>
        <div class="mt-5 flex-grow flex flex-col">
          <nav class="flex-1 bg-gradient-to-r from-rose-400 via-fuchsia-500 to-indigo-500" aria-label="Sidebar">
            <!-- Current: "bg-indigo-50 border-indigo-600 text-indigo-600", Default: "border-transparent text-gray-600 hover:bg-gray-50 hover:text-gray-900" -->
            <%= link_to "Home", "/", class:"border-transparent text-gray-600 hover:bg-gray-50 hover:text-gray-900 group flex items-center px-3 py-2 text-sm font-medium border-l-4" %>
            <%= link_to "Books", "/books", class: "border-transparent text-gray-600 hover:bg-gray-50 hover:text-gray-900 group flex items-center px-3 py-2 text-sm font-medium border-l-4" %>
            <%= link_to "Matches", "/matches", class: "border-transparent text-gray-600 hover:bg-gray-50 hover:text-gray-900 group flex items-center px-3 py-2 text-sm font-medium border-l-4" %>
          </nav>
        </div>
      </div>
    </div>
    <div class="md:pl-64 flex flex-col">
      <main class="flex-1">
        <%= yield %>
      </main>
    </div>
  </div>
</body>
</html>
